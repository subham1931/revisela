import { Model } from 'mongoose';
import { Folder, FolderDocument } from './schemas/folder.schema';
import { CreateFolderDto } from './dto/create-folder.dto';
import { UpdateFolderDto } from './dto/update-folder.dto';
import { ShareFolderDto } from './dto/share-folder.dto';
import { UsersService } from '../users/users.service';
import { QuizzesService } from '../quizzes/quizzes.service';
import { EmailService } from '../email/email.service';
import { QuizDocument } from 'src/quizzes/schemas/quiz.schema';
import { DuplicateFolderDto } from './dto/duplicate-folder.dto';
import { MoveItemsDto } from './dto/move-items.dto';
export declare class FoldersService {
    private folderModel;
    private quizModel;
    private usersService;
    private quizzesService;
    private emailService;
    constructor(folderModel: Model<FolderDocument>, quizModel: Model<QuizDocument>, usersService: UsersService, quizzesService: QuizzesService, emailService: EmailService);
    create(createFolderDto: CreateFolderDto, userId: string): Promise<Folder>;
    findAll(userId: string): Promise<Folder[]>;
    findOne(id: string, userId?: string): Promise<Folder>;
    update(id: string, updateFolderDto: UpdateFolderDto, userId: string): Promise<Folder>;
    remove(id: string, userId: string): Promise<Folder>;
    addQuiz(folderId: string, quizId: string, userId: string): Promise<Folder>;
    shareFolder(id: string, shareFolderDto: ShareFolderDto, userId: string): Promise<Folder>;
    private propagateShareToDescendantsAndQuizzes;
    generateShareLink(id: string, userId: string): Promise<string>;
    private hasPermission;
    private hasAccess;
    private hasPublicAccess;
    private sendInvitationEmail;
    moveToTrash(id: string, userId: string): Promise<Folder>;
    restoreFromTrash(id: string, userId: string): Promise<Folder>;
    permanentlyDelete(id: string, userId: string): Promise<Folder>;
    findAllInTrash(userId: string): Promise<Folder[]>;
    duplicate(id: string, duplicateFolderDto: DuplicateFolderDto, userId: string): Promise<Folder>;
    toggleBookmark(id: string, userId: string, bookmarked: boolean): Promise<Folder>;
    findBookmarked(userId: string): Promise<Folder[]>;
    moveQuiz(quizId: string, moveItemsDto: MoveItemsDto, userId: string): Promise<Folder>;
    moveFolder(folderId: string, moveItemsDto: MoveItemsDto, userId: string): Promise<Folder>;
    isDescendant(folderId: string, potentialAncestorId: string): Promise<boolean>;
    private populateFolderHierarchy;
    private hasBookmarkAccess;
}
